"use client";

import { useState } from 'react';

import BasicCard from "../materialUI/taskCard";

const options = ["hi-lo", "lo-hi", "oldest", "latest"];

export default function ProjectAreaHeader() {

    const [selectedOption, setSelectedOption] = React.useState("hi-lo");

    return(
        <div>
            
        </div>
    )
}